version: '3.8'
services:
  ketchum-super-intelligence:
    build: .
    container_name: ketchum-si-platform
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
